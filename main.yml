---
- name: hello
  hosts: localhost
  gather_facts: false
  tasks:
    - name: hi shalom
      debug:
        msg: "HALLL"
        
    - name: set fact
      ansible.builtin.set_stats:
        data:
          dog: "IM a dog hello hello"
