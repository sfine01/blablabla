---
- name: hello
  hosts: localhost
  gather_facts: false
  vars:
    dest_dir: "record/clusters/{{ cluster_name }}.yml"
  tasks:

  # Clone git repo
  # set the write files as set_stat
    - name: hi shalom1
      debug:
        msg: "HALLL"
        
    - name: set fact
      ansible.builtin.set_stats:
        data:
          activity: "blop blop"
          dog: "IM a dog hello hello"

    - name: set fact
      shell:
        cmd: pwd
        
    - name: set fact
      shell:
        cmd: ls
        
    - name: 
      debug:
        msg: "{{ {{ cluster_name }}'_activities' }}"
